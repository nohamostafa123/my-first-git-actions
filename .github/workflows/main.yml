on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 'Testing folder content'
        run: ls
        
      - name: 'Testing printing'
        run: echo Noha

